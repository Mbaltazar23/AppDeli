import { StyleSheet } from "react-native";

export const MyColors = {
  primary: "#F4991A",
  secondary: "#E14D2A",
  background: "#EEEEEE",
};

export const MyStyles = StyleSheet.create({
  loading: {
    bottom: 0,
    position: "absolute",
    top: 0,
    right: 0,
    left: 0,
  },
});
